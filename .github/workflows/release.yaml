name: Release

on:
  push:
    # tags:
    #   - 'v*'
    branches:
      - development

env:
  VITE_API_URL: ${{ secrets.VITE_API_URL }}

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18.x

      - name: Install Windows SDK
        run: Invoke-WebRequest -Uri https://aka.ms/winsdk/10/latest/SdkSetup.exe -OutFile $env:TEMP\SdkSetup.exe
        shell: powershell

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 3.1

      - name: Install dependencies and deploy
        run: |
          yarn install
          yarn tauri build --debug
          # Additional deployment steps if needed

      # - name: Set Version
      #   id: set-version
      #   run: |
      #     $newVersion = "v0.0.$($env:GITHUB_RUN_NUMBER - 1)"
      #     Write-Host "VERSION=$newVersion" | Out-File $env:GITHUB_ENV -Encoding utf8
      #     Write-Host "New version: $newVersion"
      #   shell: pwsh


      - name: Create GitHub release
        id: create-new-release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.increment-version.outputs.version }}
          release_name: v${{ steps.increment-version.outputs.version }}

      - name: Upload Release Asset for msi
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-new-release.outputs.upload_url }}
          asset_path: "D:/a/sparrow-ap/sparrow-ap/src-tauri/target/debug/bundle/msi/Sparrow-app_${{ steps.increment-version.outputs.version }}_x64_en-US.msi"
          asset_name: Sparrow-app_${{ steps.increment-version.outputs.version }}_x64_en-US.msi
          asset_content_type: application/zip

      - name: Upload Release Asset for nsis
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-new-release.outputs.upload_url }}
          asset_path: "D:/a/sparrow-ap/sparrow-ap/src-tauri/target/debug/bundle/nsis/sparrow-app_${{ steps.increment-version.outputs.version }}_x64-setup.exe"
          asset_name: sparrow-app_${{ steps.increment-version.outputs.version }}_x64-setup.exe
          asset_content_type: application/zip
